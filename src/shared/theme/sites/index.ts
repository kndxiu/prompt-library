import chatgptTheme from "./chatgpt.css?inline";
import claudeTheme from "./claude.css?inline";

export const siteThemes: Record<string, string> = {
  chatgpt: chatgptTheme,
  claude: claudeTheme,
};
